# Runtime dependencies. PyTorch is installed separately from the official CUDA
# wheel index in the Dockerfile.
transformers>=4.44,<5
diffusers>=0.30,<0.36
huggingface_hub>=0.24,<1

# Web interface
gradio>=4.44,<5
fastapi>=0.112,<1
uvicorn>=0.30,<1

# Image processing and model loading
Pillow>=10.4,<12
safetensors>=0.4.5,<1
accelerate>=0.33,<2
