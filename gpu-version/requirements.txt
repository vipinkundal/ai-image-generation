# Manual GPU environment helper.
#
# For Docker builds, use the Dockerfile. For a local virtual environment, install
# PyTorch first with:
#
#   python3 -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# Then install this file.
-r requirements-no-torch.txt
